spring:
  profiles:
    active: dev


---
spring:
  profiles: prod
  application:
    name: springconfigclient
  cloud:
    config:
      uri: http://localhost:3302
      label: master
      profile: prod


---
spring:
  profiles: dev
  application:
    name: springconfigclient
  cloud:
    config:
      uri: http://localhost:3302
      label: master
      profile: dev
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest